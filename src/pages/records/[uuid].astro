---
import PageLayout from "../../layouts/PageLayout.astro";
import { page } from "../../content/page";
import { getCommunityWatchActionData } from "../../content/communityWatchData";

const runtimeEnv = (Astro.locals as { runtime?: { env?: Record<string, string | undefined> } })
  .runtime?.env;
const uuid = Astro.params.uuid ?? "";
const watchCase = await getCommunityWatchActionData(uuid, runtimeEnv);
const baseUrl = import.meta.env.BASE_URL;
const title = watchCase
  ? `${watchCase.commentId}｜${page.hero.title}`
  : `公開紀錄不存在｜${page.hero.title}`;
const description = watchCase
  ? `守望相助隊公開紀錄：${watchCase.commentId}，處理理由為${watchCase.reason}。`
  : "守望相助隊公開紀錄頁。這筆紀錄目前無法於公開 API 查詢。";
const mailSubject = encodeURIComponent(
  `守望相助隊申訴：${watchCase?.commentId ?? uuid}`
);
const reasonVariant = watchCase?.reason === "色情廣告" ? "adult" : "spam";
const reviewTone =
  watchCase?.reviewStatus === "已恢復"
    ? "positive"
    : watchCase?.reviewStatus === "待覆核"
      ? "pending"
      : "neutral";
const appealTone = watchCase?.appealStatus === "未申訴" ? "muted" : "pending";
Astro.response.headers.set("cache-control", "no-store");
---

<PageLayout
  title={title}
  description={description}
  lang={page.lang}
>
  <div class="page">
    <header class="page__header">
      <a class="page__brand" href={baseUrl}>
        <img src={`${baseUrl}matters-lettering-black.svg`} alt="Matters" />
        <span aria-hidden="true"></span>
        <strong>守望相助隊</strong>
      </a>
      <nav class="page__nav" aria-label="主要導覽">
        <a href={`${baseUrl}#watch-log`}>處理紀錄</a>
        <a href={`${baseUrl}#appeal`}>申訴</a>
        <a href="https://matters.town/billboard">Billboard</a>
      </nav>
    </header>

    <main class="record-main">
      <a class="back-link" href={watchCase ? `${baseUrl}#case-${watchCase.id}` : baseUrl}>
        回到處理紀錄
      </a>

      {
        watchCase ? (
          <article class="record board">
            <header class="record__hero">
              <div class="record__hero-copy">
                <p class="record__kicker">
                  <span>公開紀錄</span>
                </p>
                <h1 class="record-title">{watchCase.publicNotice}</h1>
                <div class="record__status-list" aria-label="主要處理狀態">
                  <span class="status-pill status-pill--reported">已被檢舉</span>
                  <span class={`reason reason--${reasonVariant}`}>{watchCase.reason}</span>
                  {watchCase.reportSynced && (
                    <span class="status-pill status-pill--synced">
                      已同步報告問題給站方
                    </span>
                  )}
                  <span class={`status-pill status-pill--${reviewTone}`}>{watchCase.reviewStatus}</span>
                </div>
                <p class="record__source-chip" aria-label={`苦主：${watchCase.sourceTitle}`}>
                  <span>苦主</span>
                  <strong>{watchCase.sourceTitle}</strong>
                </p>
              </div>
              <aside class="record__appeal-status" aria-label="申訴狀態">
                <span>申訴狀態</span>
                <strong class={`status-text status-text--${appealTone}`}>{watchCase.appealStatus}</strong>
              </aside>
            </header>

            <div class="record-layout">
              <div class="record-layout__main">
                <section class="record-section content-box" aria-labelledby="original-content">
                  <div class="content-box__header">
                    <div class="section-heading">
                      <p class="eyebrow">原文檢視</p>
                      <h2 id="original-content">原留言內容</h2>
                    </div>
                    <button
                      class="btn btn--ghost content-box__toggle"
                      type="button"
                      data-watch-toggle
                      aria-expanded="false"
                    >
                      顯示全文
                    </button>
                  </div>
                  <p class="content-box__body is-blurred" data-watch-body>{watchCase.commentPreview}</p>
                  <p class="content-box__hint">
                    原文預設遮蔽，避免垃圾內容被二次散播；點開僅供申訴、覆核與社群稽核使用。
                  </p>
                </section>

                <section class="record-section context-panel" aria-labelledby="context-title">
                  <div class="section-heading">
                    <p class="eyebrow">處理脈絡</p>
                    <h2 id="context-title">來源與處理資訊</h2>
                  </div>
                  <dl class="context-list">
                    <div>
                      <dt>來源類型</dt>
                      <dd>{watchCase.sourceType}</dd>
                    </div>
                    {watchCase.sourceUrl && (
                      <div>
                        <dt>站上位置</dt>
                        <dd>
                          <a href={watchCase.sourceUrl} rel="noopener noreferrer">
                            回到被檢舉留言
                          </a>
                        </dd>
                      </div>
                    )}
                    <div>
                      <dt>處理人</dt>
                      <dd>{watchCase.watcher}</dd>
                    </div>
                    <div>
                      <dt>處理時間</dt>
                      <dd>{watchCase.handledAt}</dd>
                    </div>
                    <div>
                      <dt>原處替換</dt>
                      <dd>已顯示</dd>
                    </div>
                  </dl>
                </section>
              </div>

              <aside class="record-layout__side" aria-label="稽核與申訴">
                <section class="side-panel audit-box" aria-labelledby="audit-title">
                  <div class="section-heading">
                    <p class="eyebrow">稽核資訊</p>
                    <h2 id="audit-title">查詢用 ID</h2>
                  </div>
                  <dl class="audit-list">
                    <div>
                      <dt>公開紀錄 ID</dt>
                      <dd><code>{watchCase.id}</code></dd>
                    </div>
                    <div>
                      <dt>留言 ID</dt>
                      <dd><code>{watchCase.commentId}</code></dd>
                    </div>
                    <div>
                      <dt>來源 ID</dt>
                      <dd><code>{watchCase.sourceId}</code></dd>
                    </div>
                  </dl>
                </section>

                <section class="side-panel appeal-box" aria-labelledby="appeal-title">
                  <p class="eyebrow">申訴</p>
                  <h2 id="appeal-title">需要覆核？</h2>
                  <p>
                    請將留言 ID <strong>{watchCase.commentId}</strong> 寄至
                    <a href={`mailto:${page.appeal.email}?subject=${mailSubject}`}>{page.appeal.email}</a>。
                    站方會依公開紀錄、處理理由與原文進行覆核。
                  </p>
                </section>
              </aside>
            </div>
          </article>
        ) : (
          <article class="record board">
            <header class="record__hero record__hero--empty">
              <div>
                <p class="eyebrow">公開紀錄</p>
                <h1 class="record-title">找不到這筆公開紀錄</h1>
              </div>
            </header>
            <p class="record__notice">
              這筆紀錄可能尚未同步至公開 API，或已不是有效的守望相助隊處理紀錄。
            </p>
          </article>
        )
      }

      {!watchCase && (
        <aside class="appeal-box board" aria-labelledby="appeal-title">
          <p class="eyebrow">申訴</p>
          <h2 id="appeal-title">認為這則留言被誤判？</h2>
          <p>
            請將留言 ID <strong>{uuid}</strong> 寄至
            <a href={`mailto:${page.appeal.email}?subject=${mailSubject}`}>{page.appeal.email}</a>。
            站方會依公開紀錄、處理理由與原文進行覆核。
          </p>
        </aside>
      )}
    </main>

    <footer class="page__footer">
      <div class="page__footer-copy">
        <span>{page.footer.note}</span>
        <strong>{page.footer.warning}</strong>
      </div>
    </footer>
  </div>
</PageLayout>

<script>
  document.querySelectorAll("[data-watch-toggle]").forEach((button) => {
    button.addEventListener("click", () => {
      const content = button.closest(".content-box")?.querySelector("[data-watch-body]");
      const isExpanded = button.getAttribute("aria-expanded") === "true";

      content?.classList.toggle("is-blurred", isExpanded);
      button.setAttribute("aria-expanded", String(!isExpanded));
      button.textContent = isExpanded ? "顯示全文" : "收起全文";
    });
  });
</script>

<style>
  .record-main {
    width: 100%;
    max-width: var(--board-max);
    margin: 0 auto;
    padding: var(--space-watch-24) var(--board-pad-x) var(--space-watch-48);
    display: grid;
    gap: var(--space-sp16);
  }

  .back-link {
    color: var(--ink-muted);
    font-weight: 700;
    text-decoration: none;
  }

  .back-link:hover {
    color: var(--brand-purple);
  }

  .record__hero {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: end;
    gap: var(--space-watch-24);
    border-bottom: 1px solid var(--line);
    padding-bottom: var(--space-watch-24);
  }

  .record__hero-copy {
    min-width: 0;
  }

  .record__kicker {
    margin: 0 0 var(--space-sp10);
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: var(--space-sp8);
    color: var(--ink-muted);
    font-size: 14px;
    font-weight: 800;
  }

  .record__kicker span {
    display: inline-flex;
    align-items: center;
    gap: var(--space-sp8);
  }

  .record__kicker span + span::before {
    content: "";
    width: 4px;
    height: 4px;
    border-radius: 999px;
    background: var(--line);
  }

  .record h1 {
    margin: 0;
    font-family: var(--font-reading);
    font-size: 32px;
    font-weight: 600;
    line-height: 54px;
    letter-spacing: 1.2px;
    overflow-wrap: anywhere;
  }

  .record__status-list {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-sp8);
    margin-top: var(--space-sp14);
  }

  .record__source-chip {
    display: inline-flex;
    max-width: 100%;
    align-items: center;
    gap: var(--space-sp8);
    margin: var(--space-sp12) 0 0;
    border: 1px solid var(--line);
    border-radius: 6px;
    padding: var(--space-sp4) var(--space-sp8);
    background: var(--surface-muted);
    color: var(--ink-muted);
    font-size: 14px;
    line-height: 1.4;
  }

  .record__source-chip span {
    flex: 0 0 auto;
    color: var(--brand-purple);
    font-size: 13px;
    font-weight: 800;
  }

  .record__source-chip strong {
    min-width: 0;
    overflow: hidden;
    font-weight: 700;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .record__appeal-status {
    min-width: 160px;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: var(--surface-muted);
    padding: var(--space-sp14) var(--space-sp16);
  }

  .record__appeal-status span {
    display: block;
    color: var(--ink-muted);
    font-size: 12px;
    font-weight: 600;
    line-height: 18px;
  }

  .record__appeal-status strong {
    display: block;
    margin-top: var(--space-sp4);
    font-size: 18px;
    line-height: 28px;
  }

  .context-list dt,
  .audit-list dt {
    margin: 0;
    color: var(--ink-muted);
    font-size: 12px;
    font-weight: 600;
    line-height: 18px;
  }

  .context-list dd,
  .audit-list dd {
    margin: var(--space-sp4) 0 0;
    color: var(--ink);
    font-size: 16px;
    font-weight: 600;
    line-height: 24px;
    overflow-wrap: anywhere;
  }

  .reason {
    display: inline-flex;
    min-height: 30px;
    align-items: center;
    border-radius: 999px;
    padding: 4px 10px;
    font-size: 13px;
    font-weight: 600;
    line-height: 1.3;
  }

  .reason--adult {
    background: var(--adult-bg);
    color: var(--adult-ink);
  }

  .reason--spam {
    background: var(--spam-bg);
    color: var(--spam-ink);
  }

  .status-pill {
    display: inline-flex;
    min-height: 30px;
    align-items: center;
    border-radius: 999px;
    padding: 4px 10px;
    font-size: 13px;
    font-weight: 700;
    line-height: 1.3;
  }

  .status-pill--reported {
    background: var(--surface-purple);
    color: var(--brand-purple);
  }

  .status-pill--synced {
    background: #eef2ff;
    color: #3730a3;
  }

  .status-pill--positive {
    background: var(--color-secondary-0);
    color: var(--color-function-positive-dark, #0d7a3f);
  }

  .status-pill--pending {
    background: var(--surface-purple);
    color: var(--brand-purple);
  }

  .status-pill--neutral {
    border: 1px solid var(--line);
    background: var(--board-bg);
    color: var(--ink);
  }

  .record-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 320px);
    gap: var(--space-watch-24);
    margin-top: var(--space-watch-24);
    align-items: start;
  }

  .record-layout__main {
    min-width: 0;
  }

  .record-layout__side {
    display: grid;
    gap: var(--space-sp16);
    min-width: 0;
  }

  .side-panel,
  .context-list,
  .audit-list {
    margin: 0;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: var(--board-bg);
  }

  .side-panel {
    padding: var(--space-sp16);
  }

  .context-list div,
  .audit-list div {
    min-width: 0;
    padding: var(--space-sp14) var(--space-sp16);
    border-bottom: 1px solid var(--line);
  }

  .context-list div:last-child,
  .audit-list div:last-child {
    border-bottom: 0;
  }

  .status-text {
    color: var(--ink);
  }

  .status-text--positive {
    color: var(--color-function-positive-dark, #0d7a3f);
  }

  .status-text--pending {
    color: var(--brand-purple);
  }

  .status-text--muted {
    color: var(--ink-muted);
  }

  .record-section {
    margin-top: 0;
  }

  .section-heading {
    margin-bottom: var(--space-sp12);
  }

  .section-heading h2,
  .content-box__header {
    margin: 0;
  }

  .section-heading h2,
  .appeal-box h2,
  .side-panel h2 {
    font-family: var(--font-ui);
    font-size: 20px;
    font-weight: 600;
    line-height: 30px;
  }

  .section-heading h2 {
    margin: 0;
  }

  .context-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .context-list div {
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .context-list div:nth-child(2n) {
    border-right: 0;
  }

  .context-list div:nth-last-child(-n + 2) {
    border-bottom: 0;
  }

  .content-box__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-sp12);
  }

  .content-box__body {
    min-height: 172px;
    margin: var(--space-sp12) 0 0;
    border: 1px solid var(--line);
    border-radius: 8px;
    background: var(--surface-muted);
    padding: var(--space-sp16);
    transition: filter 0.16s ease, opacity 0.16s ease;
  }

  .content-box__body.is-blurred {
    filter: blur(7px);
    opacity: 0.56;
    user-select: none;
  }

  .content-box__toggle {
    font-size: 14px;
    line-height: 22px;
    min-height: 40px;
    padding: var(--space-sp8) var(--space-sp16);
  }

  .content-box__hint,
  .appeal-box p {
    color: var(--ink-muted);
  }

  .audit-list code {
    font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
    font-size: 13px;
    white-space: normal;
    word-break: break-all;
  }

  .appeal-box a {
    font-weight: 800;
    text-decoration: none;
  }

  @media (max-width: 900px) {
    .record__hero,
    .record-layout {
      grid-template-columns: 1fr;
    }

    .record__appeal-status {
      width: fit-content;
      min-width: 0;
    }

    .context-list {
      grid-template-columns: 1fr;
    }

    .context-list div {
      border-right: 0;
      border-bottom: 1px solid var(--line);
    }

    .context-list div:nth-child(2n) {
      border-right: 0;
    }

    .context-list div:nth-last-child(-n + 2) {
      border-bottom: 1px solid var(--line);
    }

    .context-list div:last-child {
      border-bottom: 0;
    }
  }

  @media (max-width: 640px) {
    .record__hero {
      display: grid;
    }

    .content-box__header {
      align-items: flex-start;
      flex-direction: column;
    }

    .record h1 {
      font-size: 24px;
      line-height: 36px;
    }
  }
</style>
